---
import Grid from '~/components/blog/Grid2.astro';
// import Headline from '~/components/blog/Headline.astro';
import { findLatestPosts } from '~/utils/blog';

export interface Props {
  title?: string;
  allPostsText?: string;
  allPostsLink?: string | URL;
  information?: string;
  postIds: string[];
  count?: number;
}

const {
  count = 6,
} = Astro.props;

const posts = await findLatestPosts({ count });
---

<section class="py-12 sm:py-16 lg:py-20 w-full">
  <!-- <Headline
    subtitle="Encuentra todas nuestras publicaciones y podcast con actulizaciones constantes. Te invitamos a debatir las tematicas en el foro interactivo."
  >
    Blog
  </Headline> -->

  <Grid posts={posts} />
</section>